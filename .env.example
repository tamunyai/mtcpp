# Example environment variables for mtcpp
# Copy this file to `.env` and adjust values for your environment.

# App Settings
DEBUG=false
PROD=false

# Security & JWT
SECRET_KEY=super-secret-key
ALGORITHM=HS256

# Token lifetimes (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=1440

# Optional: override database URL. If unset the app uses the default sqlite path under `data/`.
# DATABASE_URL=sqlite:///data/application.db

# Optional logging directory override
# LOG_DIR=logs
